alias t="tmux"
alias ta="tmux a -d"
alias rm='rm -i'
alias mv='mv -i'
alias cp='cp -i'
alias e='emacs'

alias -g P="| peco"
alias pls="find . -type f -follow| peco | xargs less -NM"

# For Mac OS X only
alias fp="find . -follow| peco | tr -d '\n' | pbcopy"

alias javac='javac -J-Dfile.encoding=utf-8'
alias java='java -Dfile.encoding=utf-8'

alias gdns='ping 8.8.8.8'

alias g='git'
alias gpo='git push origin'
alias gfo='git fetch origin'
alias gc='git ci'
# alias gg='git gr'
alias gg='echo "Use ag instead."'
alias gh='git log -1 --format=format:%h | pbcopy'
alias gpc='git branch | percol | xargs git checkout'

alias be='bundle exec'
alias ber='bundle exec rails'
alias bes='bundle exec spring'
alias besr='bundle exec spring rails'

alias less="less -MN"
alias rak="rak -C 2"

alias -g SAY="&& (say 'Success' && terminal-notifier -message 'Success' ) || (say 'Failed' && terminal-notifier -message 'Failed')"
alias -g IM="&& (say 'Success' && terminal-notifier -message 'Success' && notify_kayac 'Success') || (say 'Failed' && terminal-notifier -message 'Failed' && notify_kayac 'Failed' )"

alias o='F=$(find . -follow| percol); open ${F}; echo ${F}'

alias ag='ag --pager="less -F -R" -A 3 -B 3'
